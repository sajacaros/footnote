import 'package:flutter_test/flutter_test.dart';

import 'package:footnote_walk/main.dart';

void main() {
  test('app widget can be constructed', () {
    expect(const FootnoteWalkApp(), isA<FootnoteWalkApp>());
  });
}
